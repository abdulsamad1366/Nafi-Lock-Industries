/**
 * Admin — Brands CRUD page.
 * Manage brand name, tagline, theme, order, active status.
 */
export default function AdminBrandsPage() {
  return (
    <div>
      <h1 className="font-headline text-2xl mb-6">Manage Brands</h1>
      <p className="text-muted">Brand management interface — to be implemented.</p>
    </div>
  );
}
