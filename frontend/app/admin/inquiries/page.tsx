/**
 * Admin — Inquiries management page.
 * View/manage contact form submissions, update status (new → contacted → closed).
 */
export default function AdminInquiriesPage() {
  return (
    <div>
      <h1 className="font-headline text-2xl mb-6">Inquiries</h1>
      <p className="text-muted">Inquiry management interface — to be implemented.</p>
    </div>
  );
}
